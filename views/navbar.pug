//- NAV-BAR
link(href='./../public/css/navbar.css', rel='stylesheet')
nav.navbar.navbar-light
  .container-fluid
    #nav-h.navbar-header
      a.navbar-brand(href='/events') US-MEX FoCUS
    ul.nav.navbar-nav
      li.active
        a(href='/events') My Events
      if login && user.admin==false
        li
          a(href='/schedules') Schedule Interview
    if login
      ul#nav-dd.nav.navbar-nav.pull-right
        li.dropdown
          a.dropdown-toggle(href='#',data-toggle='dropdown') #{pageData.name}
            span.caret
              ul.dropdown-menu(style='right: 0; left: auto;')
                li
                  a(href="/logout") Sign out
        
        
!= messages('message',locals)
if errors
  each error,i in errors
      div.alert.alert-danger #{error.msg}

